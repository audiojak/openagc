# Overnight run — 2026-09-26

Branch `overnight-2` from `main`. Commit and push after every closed issue.
A PR to `main` in the morning; never push to `main` directly tonight.

## Order

Work `bd ready` in this order; within an epic, follow the dependencies.

1. **oagc-c49** Label tree (spec §14.3 amendment). Small, self-contained,
   pure UI on top of existing data. Do first to warm up the loop.
2. **oagc-dy4** Multiple accounts (spec §7.7). The registry (dy4.1) and
   per-account events (dy4.2) are the foundation the next two epics need.
3. **oagc-4st** Archive accounts from mbox (spec §7.8). Depends on dy4.1.
4. **oagc-458** IMAP bulk backfill (spec §7.4 amendment). Its scope task
   (458.4) depends on dy4.4 so the consent screen changes once:
   `gmail.modify` + `userinfo.email` + `openid` + `profile` + `mail.google.com`.
5. If time remains: **oagc-d5k** server-side search outside the window,
   **oagc-e1g** orphaned-store cleanup.

Do **not** touch M0/M2/M6 (oagc-87q, oagc-en9, oagc-cp3) or the release,
verification and Dolt issues: every one of them needs the maintainer.

## Checks before closing an issue

`scripts/gate.sh` (never piped) and `scripts/test-macos.sh test`. Record
spec amendments in `docs/SPECIFICATION.md` when a decision changes; keep
`docs/mcp.md` generated (`cargo xtask mcp-docs`).

## Guardrails (in addition to CLAUDE.md)

- **Never launch the app against the real account and never start sync
  outside the fakes.** This Mac has a dev-signed build that can read the
  real Gmail token from the Keychain. Tests run with `isRunningTests`
  fakes; snapshots use `-OpenAGCFakeAgents YES` and the demo account
  (`-OpenAGCDemo`); nothing connects to `gmail.googleapis.com`,
  `imap.gmail.com`, `openidconnect.googleapis.com` or `oauth2.googleapis.com`.
- No real mbox: fixtures are generated by test helpers.
- No Google, Apple or Anthropic account actions; no cloud routines; never
  read `~/.claude/.credentials.json`, `~/.codex/auth.json` or the login
  Keychain items under service `ai.actual.openagc`.
- Do not delete anything under `~/Library/Application Support/OpenAGC`.
- If a step needs a decision the spec does not settle, choose the option
  closest to the spec's text, note it in the issue and in the commit, and
  carry on; list all such decisions in the morning handoff.

## Morning handoff

Open the PR, list closed issues, spec amendments, decisions taken, and
anything left half-done with its `bd` notes.
